import React from "react";
import Card from "../base/card";

const CompetencyTechnology = ({ title, competencytechnology }) => {
  return <Card title={title}>{competencytechnology}</Card>;
};

export default CompetencyTechnology;
